<?xml version="1.0" encoding="utf-8"?>
<!-- Development only: Android 9+ refuses cleartext http everywhere, which
     would stop an app built against http://10.0.2.2 (the emulator's alias
     for the host machine) from loading its API or streaming audio through
     ExoPlayer. Cleartext is permitted for those two local names alone;
     every other host keeps the platform default and needs TLS. The iOS
     shell's NSAllowsLocalNetworking is the same rule. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
