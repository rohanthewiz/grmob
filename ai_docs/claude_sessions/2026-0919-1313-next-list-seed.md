# Next list seeded as a living file

**Session:** 5d161579-c1e2-4099-9d7c-6afbcf6115fb
**Date:** 2026-09-19 13:13 (follows "fold6-talkback-hid-harness-focus-after-navigation-inert-named-controls", same session)
**Branch:** master (c1829c4 → this commit)

## The ask

1. `/sw` after the Fold6 work: saved `2026-0919-1254-…`, committed and pushed
   as c1829c4.
2. `/next-list seed`: build the living list from session-doc history.
3. `/sess-save`.

## What was built

`ai_docs/todo/next-list.md`: 40 Open, 19 Non-goals, 0 Closed.
**Next ID** is N-060.

- **Source.** The item text comes from the 1254 doc's `## Next`, which held
  the full numbered list. Each item keeps `(was #n)`, so the old numbers in
  earlier docs still map across.
- **IDs.** N-001 to N-059 are assigned oldest-raised first; ties go by the
  old number.
- **`raised`.** Each item's first appearance was found by grepping a
  distinctive identifier across all 264 session docs, not the 15-doc
  window. Two cases needed a judgement:
  - **Too generic** ("on hardware", "haptics", "dark mode", "in proportion",
    "iOS 17 floor", "low-hanging", "examples/chat"): each earliest hit was
    read in context. Item 1's "not on hardware" (0912-1821) is the same
    thread and was kept; the others were a different topic and lost to the
    doc the item's age pointed to.
  - **Wording drifted** ("two-segment", "strands its schedules",
    "Continue apps on cover screen", "guide and demo halves", "caret
    correction"): the grep found them later than the age says, so the age's
    doc was used.
- **Cross-references** to old numbers (items 35, 62, 64, 67, 77) were
  rewritten as IDs. The 1254-relative "§n" references now name that doc.
- **Non-goals.** For the seed, `declined` is the doc the item was first
  raised in, since some were declined later. The Conventions section says so.
- **Closed.** Nothing was found done during the rebuild. The section points
  to the session docs for earlier closures (1254: #5, #62, #71, #75; 1118:
  #7, #25, #70, #72, #73).

## Checks

- **Lapses, window 0917-2001 to 0919-1254.** Item numbers were compared
  across the numbered docs since the 0918-2203 rebuild (2203, 2310, 0443,
  1118, 1254). Every number that left a list is in the next doc's Closed
  list:
  - 0443: 4, 19, 29, 31–33, 42, 54, 57, 58, 60, 61, 66.
  - 1118: 7, 25, 70, 72, 73.
  - 1254: 5, 62, 71, 75.

  None lapsed. The earlier drop at 0917-1659 was already restored by the
  2203 rebuild and is kept as `lapsed@0917-1659`.
- **Premises, all still true:**
  - `DefaultDarkChartColors` is defined only in `core/theme.go`.
  - `core.LinearGradient` has no user outside core.
  - `core/` has no `SafeInsets`.
  - The Compose BOM is still 2024.12.01.
  - `barValueRoom` still divides by `barPlotGuess`.
  - `core/canvas.go` still documents no text.
  - `mobile.SetTimeZone` still sets `time.Local` once.

  No value changed.
- **Waiting on the user:**
  - Three delete candidates: N-016 (double-post), N-019
    (`TutorialChartsUITests` failed once), N-036 (a sixth low-hanging round).
  - Three proposed non-goals: N-011 (English chart summaries), N-022 (zero
    basis on iOS), N-025 (Sparkline's flat Area).

## Files

- `ai_docs/todo/next-list.md` (new).

## Next

Closed: None. Declined: None. Raised: None.
Updated: None. Full list: `ai_docs/todo/next-list.md` (seeded this session,
N-001 to N-059).
