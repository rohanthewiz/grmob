// grmob-shot: {"app": "counter", "w": 414, "h": 260}
//
// docs/images/counter.png — the README's first app, after three taps.
//
// The frame is short on purpose. A counter is three controls, and a
// phone-height bezel around them is four hundred pixels of blank paper
// that a reader has to scroll past to reach the next paragraph.
await tap("+");
await tap("+");
await tap("+");
