// grmob-shot: {"app": "tutorial", "w": 414, "h": 800}
//
// docs/images/tutorial-contents.png — the contents screen as it opens.
//
// No actions at all, and that is the state: chapter 1 is expanded by the
// app rather than by this script, and the progress caption says zero
// because nothing has been visited. Driving anything here would make the
// picture a picture of the driving.
//
// The empty body is deliberate. A script that did nothing but exist would
// be a file with a header in it, and the header is what the driver needs:
// which app, and how big the frame is.
